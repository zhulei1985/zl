void main()
{
	Client pClient = NewClient("127.0.0.1",9999);

	while (pClient->IsConnect()==0)
	{
		wait(1);
	}
	print("connect...ok");
	int nVal = pClient->RunScript(1,"Test_Add", 1,2);
	print(nVal);
}